{
  "name": "conta-finance-pwa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "build": "node scripts/build.js",
    "check": "node --check server/index.js && node --check server/auth.js && node --check server/store.js && node --check server/finance.js && node --check server/integrations.js && node --check server/payments.js && node --check public/app.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
